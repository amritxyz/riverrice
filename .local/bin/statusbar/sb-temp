#!/bin/sh

case $BLOCK_BUTTON in
	6) setsid -f "$TERMINAL" -e "$EDITOR" "$0" ;;
esac

temp_milli=$(cat /sys/class/hwmon/hwmon5/temp1_input)
temp_celsius=$((temp_milli / 1000))

echo " $temp_celsius°C"
